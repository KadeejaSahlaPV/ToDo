 {toDos.some((obj) => obj.status) && (
          <div>
            <br/><br/><br/><h2>Completed Tasks</h2><br/>
 
  { toDos.map((obj)=>{
    if(obj.status){
      return(<h3 className="h3" key={obj.id} >{obj.text}</h3>)
    }
    return null
  })}
        </div>
        
      )}

      SEARCH

      export function search(ToDos, query) {
    let q = query.trim().toLowerCase();

    return ToDos.filter(({text}) => stringInclues(text.toLowerCase(), q));
}